syntax = "proto3";



package pipe;


service Transfer {
        rpc AddUpdateRecord (stream Record) returns (stream Ack){}
}

message Record {
    int64 id = 1;
    string name = 2;
    string email = 3;
    string phone = 4;
}


message Ack {
    bool   err    =1;
    string message =2 ;
}