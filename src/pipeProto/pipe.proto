syntax = "proto3";

package pipe;

import "google/protobuf/empty.proto";

service Transfer {
        rpc AddUpdateRecord (Record) returns (google.protobuf.Empty){}
}

message Record {
    int64 id = 1;
    string name = 2;
    string email = 3;
    string phone = 4;
}

